creditflow {
  http {
    host = "0.0.0.0"
    port = 8081
  }
  db {
    url = "jdbc:postgresql://localhost:5432/users_db"
    user = "creditflow"
    password = "creditflow"
    pool-size = 10
  }
  jwt {
    secret = "change-me-in-production-use-long-random-secret"
    ttl-seconds = 3600
  }
}
