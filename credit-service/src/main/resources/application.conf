creditflow {
  http {
    host = "0.0.0.0"
    port = 8083
  }
  db {
    url = "jdbc:postgresql://localhost:5432/credit_db"
    user = "creditflow"
    password = "creditflow"
  }
  kafka {
    bootstrap-servers = "localhost:9092"
    group-id = "credit-service"
  }
}
